version: "3.9"

services:
  backend:
    build:
      context: ./onkyo-api
      dockerfile: Dockerfile
    container_name: onkyo-api
    volumes:
      - onkyo-api-data:/app
    ports:
      - "0.0.0.0:8001:8000"

volumes:
  onkyo-api-data:
